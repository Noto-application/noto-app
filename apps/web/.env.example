NEXT_PUBLIC_API_URL=http://localhost:4000

# Единый origin (dev): фронт ходит в API и collab через Caddy :8080.
# NEXT_PUBLIC_API_URL=http://localhost:8080

# Collab-режим редактора (#110): спайк-флаг, по умолчанию выключен.
# Пользовательское включение — только после #109 (см. collab-editing.spec.md).
NEXT_PUBLIC_COLLAB_ENABLED=false
