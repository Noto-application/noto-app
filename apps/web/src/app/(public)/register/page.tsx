import { RegisterForm } from '@/src/features/auth';

/** RSC-маршрут: интерактивность и Query живут исключительно внутри формы. */
export default function RegisterPage() {
  return <RegisterForm />;
}
